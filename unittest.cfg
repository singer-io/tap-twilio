[unittest]
start-dir = tests/unittests
code-directories = .
test-file-pattern = test_*.py

[log-capture]
always-on = True
clear-handlers = True
filter = -nose

[coverage]
always-on = False
coverage = tap_twilio
coverage-report = term-missing
